$(document).ready(function(){


  // Semantic UI Range
  $('.ui.range').range({
    min: 0,
    max: 10,
    start: 5,

  });

  $('#smooth').range({
    min: 0,
    max: 10,
    start: 5,

  });

  $('#range-0').range({
    min: 0,
    max: 10,
    start: 5,

  });

});
